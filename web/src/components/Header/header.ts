import styled from 'styled-components';

export const HeaderContainer = styled.header`
  background-color: rgba(82, 5, 155, 0.8);
  width: 100vw;
  height: 5rem;
  display: flex;
  align-items: center;
  justify-content: center;
`;